pipeline{
 agent any
 environment{
   USERNAME = 'sidsin25'
   }
   stages{
    stage('Example'){
      environment{
       API_KEY = 'ISCP123'
       }
       steps{
       sh 'printenv'
stages{
      stage('Build'){
       steps{
        sh 'ls'
        checkout scm
        }
       }
        stage('Deploy'){
         steps{
         sh 'pwd'
         }
        }
       }
      }
